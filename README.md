# Recall — an adaptive study assistant

Paste your notes or name a topic. Recall turns it into flashcards or a quiz,
generated by an LLM (Groq) as structured JSON — not a chat transcript you
have to scroll through.

The thing that makes this more than a flashcard-printer: cards aren't
"correct/wrong once and done." Each card tracks a **mastery streak** — rate
it "Got it" twice in a row and it graduates out of the deck. "Shaky" or
"Missed" sends it back into rotation, and you get a short message telling
you exactly what happened before the next card appears. Progress isn't
all-or-nothing either — the progress bar moves on every correct answer, not
just when a card fully masters, so early progress actually feels like
progress.

## Setup

```bash
npm install
cp .env.example .env      # then paste your Groq API key into .env
npm start                  # runs at http://localhost:5173
```
Get a free Groq API key at https://console.groq.com/keys — no cost for the
model used here (`llama-3.3-70b-versatile`).

The dev server *is* the backend: `vite.config.js` runs a small middleware
(`devApiPlugin`) that intercepts `POST /api/generate` inside the same Node
process and calls Groq server-side. The API key is read from `.env` on the
server and never reaches the browser bundle. `api/generate.js` is the same
logic packaged as a Vercel serverless function for a real deploy — both
entry points share `lib/groq.js` and `lib/schema.js`, so there's one source
of truth for the AI call and validation instead of two copies that could
drift apart.

## Architecture
InputScreen → useStudySet (state machine) → api.js → /api/generate → lib/groq.js → Groq API
↓
lib/schema.js (Zod)
↓
validated data or typed error

## Handling bad AI output

| Failure mode | Where it's caught | What the user sees |
|---|---|---|
| Non-JSON / malformed response | `validateStudySet` in `lib/schema.js`, `JSON.parse` wrapped in try/catch | One automatic repair retry with a stricter prompt; if that also fails, a clear error + retry button |
| Right JSON, wrong shape (missing fields, wrong types) | Zod schema validation (`StudySetSchema`) | Same repair-retry path, then error state |
| Empty item list | Explicit length check in `validateStudySet` | "No study items could be generated — try adding more detail" |
| Slow / hanging request | Client-side 25s timeout via `AbortController` in `useStudySet.js` | Loading indicator, then a timeout-specific error message |
| Newer request started before an older one resolves | `requestIdRef` counter in `useStudySet.js` — any response for a stale id is silently dropped | The screen only ever reflects the most recent request; no flicker back to old data |
| Network / provider failure | try/catch around `fetch` in `lib/groq.js` and `api.js` | Error state with retry, no crash |
| Missing API key | Checked explicitly in `generateStudySet` before calling Groq | Clear setup-error message instead of a generic failure |

## AI usage (Claude)

- **Scaffolding**: config boilerplate — Vite/Tailwind setup, Google Fonts links.
- **Backend middleware**: first draft of the Vite dev-server middleware (`devApiPlugin`) — I hadn't built one before, had it explained line by line before using it.
- **Prompt drafting**: initial Groq system prompt, then iterated manually after real failures (e.g. model wrapping JSON in code fences).
- **Race-condition fix**: knew the bug existed, didn't know the fix pattern — Claude walked me through `requestIdRef`, I implemented and tested it myself.
- **Visual direction**: gave a reference screenshot, asked for the same gradient/typography language applied here.
- **UX iteration**: my first mastery-loop gave no feedback on wrong answers and only moved the progress bar on full mastery — I found this while testing, described the problem, and implemented the fix (granular progress + feedback messages) myself with Claude's input on the approach.

## What I built and decided myself

- The Zod schema shape and validation rules (`lib/schema.js`)
- The retry-on-failure control flow (`lib/groq.js`)
- The mastery-streak state machine — streak tracking, requeue-to-back-of-queue, graduation logic (`StudyDeck.jsx`)
- The failure-mode table above, and the decision to retry only once rather than indefinitely
- The mastery-loop concept itself (inspired by spaced-repetition apps like Anki, simplified to a streak counter instead of time-based intervals)

## Known limitations

- Quiz mode auto-rates correctness against `correctIndex`; flashcard mode is
  self-rated (honor system) — there's no way to verify a "Got it" claim on
  a flashcard beyond trusting the user.
- No persistence yet — refreshing the page loses progress on the current
  deck.
- The repair-retry in `lib/groq.js` only retries once, deliberately — a
  model that consistently fails validation surfaces a clear error instead
  of retrying indefinitely against the API.
- Tested primarily against `llama-3.3-70b-versatile` on Groq. Smaller or
  local models (e.g. via Ollama) will hit the repair-retry path more often
  since they're less consistent at structured output — the failure-handling
  table above is what absorbs that difference.

## If I had more time

- Persist sessions (localStorage or a lightweight backend) so a deck
  survives a page refresh.
- Stream the generation response in as cards arrive instead of waiting for
  the full set to come back.
- Let users edit a card's question/answer inline instead of only rating it.
- Keyboard shortcuts for rating (1/2/3) for faster review sessions.

## Time spent

~8 hours: roughly 1.5h on architecture and product decisions, 3h on the
core build (dev-server middleware, schema validation, API layer), 2.5h on
the mastery-loop UI, styling, and UX fixes, and 1h on testing failure paths
and writing this README.
